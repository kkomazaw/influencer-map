# Node.js環境設定
NODE_ENV=development  # 'development' | 'production' | 'test'
PORT=4000             # APIサーバーのポート番号

# Firebase設定
FIREBASE_PROJECT_ID=influencer-map-dev  # FirebaseプロジェクトID

# 本番環境のみ設定（開発環境では不要 - Emulator使用）
# FIREBASE_SERVICE_ACCOUNT='{"type":"service_account","project_id":"xxx",...}'

# CORS設定
CORS_ORIGIN=http://localhost:5173  # 許可するオリジン（カンマ区切り可）

# 使用方法:
# 1. このファイルを .env にコピー
# 2. 各値を実際の設定に置き換え
# 3. .env ファイルは .gitignore に含まれているため、Gitにコミットされません

# 開発環境（Firebase Emulator使用）:
#   NODE_ENV=development
#   FIREBASE_PROJECT_ID=influencer-map-dev
#   FIREBASE_SERVICE_ACCOUNT は未設定でOK

# 本番環境（実Firestore使用）:
#   NODE_ENV=production
#   FIREBASE_PROJECT_ID=your-actual-project-id
#   FIREBASE_SERVICE_ACCOUNT='{"type":"service_account",...}'

# 詳細は docs/ENVIRONMENT_SETUP.md を参照してください
