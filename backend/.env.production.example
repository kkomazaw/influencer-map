# Production Environment Variables for Render
# これらの値はRenderの環境変数設定で設定してください

# Node.js環境設定
NODE_ENV=production
PORT=4000

# Firebase Admin SDK設定（必須）
# Firebase Console > Project Settings > Service Accounts で取得
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com

# CORS設定（必須）
# Vercelデプロイ後のフロントエンドURLを指定
# 例: https://your-app.vercel.app
ALLOWED_ORIGINS=https://your-frontend-url.vercel.app

# 注意事項:
# 1. FIREBASE_PRIVATE_KEY の改行は \n でエスケープする必要があります
# 2. Renderダッシュボードで環境変数を設定する際、値に引用符は不要です
# 3. ALLOWED_ORIGINS には実際のVercelデプロイURLを設定してください
